<h1>Tasks</h1>

<table>
	<tr>
		<th>Title</th>
		<th>Completed</th>
	</tr>

	<% tasks.each(function(model)({ %>
		<tr>
			<td><%= title %></td>
			<td><%= completed %></td>
		</tr>
	<% }); %>
</table>